# Validation

<!-- This validation file cannot be generated yet because the Jovian hardfork functions
     (setDAFootprintGasScalar and setMinBaseFee) don't exist on the current SystemConfig
     deployment on Base Sepolia.

     Run `make gen-validation` AFTER the Jovian upgrade has been deployed to generate
     this file automatically. -->
